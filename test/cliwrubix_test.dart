import 'package:cliwrubix/cliwrubix.dart';
import 'package:test/test.dart';

void main() {
  test('new', () {
    // expect(calculate(), 42);
  });
  test('sign', () {
    // expect(calculate(), 42);
  });
  test('bal', () {
    // expect(calculate(), 42);
  });
}
