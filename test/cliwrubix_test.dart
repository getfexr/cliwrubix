import 'package:cliwrubix/cliwrubix.dart';
import 'package:test/test.dart';

void main() {
  test('new', () {
    // expect(newDID(), 42);
  });
  test('sign', () {
    // expect(signTransaction(), 42);
  });
  test('bal', () {
    // expect(checkBalance(), 42);
  });
}
